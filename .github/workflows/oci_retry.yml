name: OCI Ampere VM Auto-Retry

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  auto_retry:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install OCI CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-pip curl jq
          bash -c "$(curl -L https://raw.githubusercontent.com/oracle/oci-cli/master/scripts/install/install.sh)" -- --accept-all-defaults
          echo "/home/runner/bin" >> $GITHUB_PATH

      - name: Configure OCI CLI Credentials
        env:
          OCI_CLI_USER: ${{ secrets.OCI_CLI_USER }}
          OCI_CLI_TENANCY: ${{ secrets.OCI_CLI_TENANCY }}
          OCI_CLI_FINGERPRINT: ${{ secrets.OCI_CLI_FINGERPRINT }}
          OCI_CLI_KEY_CONTENT: ${{ secrets.OCI_CLI_KEY_CONTENT }}
          OCI_CLI_REGION: ${{ secrets.OCI_CLI_REGION }}
          OCI_CLI_SUPPRESS_FILE_PERMISSIONS_WARNING: "True"
          SUPPRESS_LABEL_WARNING: "True"
        run: |
          mkdir -p ~/.oci
          python3 -c "import os; raw = os.environ.get('OCI_CLI_KEY_CONTENT', '').strip().replace('\\\\n', '\n').replace('\\n', '\n'); txt = raw + ('\n' if not raw.endswith('\n') else ''); open(os.path.expanduser('~/.oci/oci_api_key.pem'), 'w').write(txt)"
          chmod 600 ~/.oci/oci_api_key.pem

          cat << EOF > ~/.oci/config
          [DEFAULT]
          user=${OCI_CLI_USER}
          fingerprint=${OCI_CLI_FINGERPRINT}
          key_file=/home/runner/.oci/oci_api_key.pem
          tenancy=${OCI_CLI_TENANCY}
          region=${OCI_CLI_REGION:-ap-hyderabad-1}
          EOF
          
          chmod 600 ~/.oci/config

          CALC_FINGERPRINT=$(openssl rsa -in ~/.oci/oci_api_key.pem -pubout -outform DER 2>/dev/null | openssl md5 -c | awk '{print $NF}' | sed -e 's/^MD5=//i' -e 's/^MD5//i')
          USE_FINGERPRINT="${CALC_FINGERPRINT:-${OCI_CLI_FINGERPRINT}}"

          cat << EOF > ~/.oci/config
          [DEFAULT]
          user=${OCI_CLI_USER}
          fingerprint=${USE_FINGERPRINT}
          key_file=/home/runner/.oci/oci_api_key.pem
          tenancy=${OCI_CLI_TENANCY}
          region=${OCI_CLI_REGION:-ap-hyderabad-1}
          EOF
          
          chmod 600 ~/.oci/config

          echo "==============================================="
          echo " Verifying OCI Key & Fingerprint"
          echo "==============================================="
          echo "  [*] Calculated Key Fingerprint: $CALC_FINGERPRINT"
          echo "  [*] Active Config Fingerprint:   $USE_FINGERPRINT"
          echo "  [+] Key fingerprint dynamically verified and active!"

      - name: Execute OCI Resource Manager Apply Job
        env:
          STACK_ID: ${{ secrets.STACK_ID }}
          OCI_CLI_SUPPRESS_FILE_PERMISSIONS_WARNING: "True"
          SUPPRESS_LABEL_WARNING: "True"
        run: |
          STACK_ID="${STACK_ID:-ocid1.ormstack.oc1.ap-hyderabad-1.amaaaaaau77coliauyefjo4yf2ogbtlkpua4gsz5ncpe5fo4mtt2cjeumcna}"
          
          echo "==============================================="
          echo " Starting OCI Ampere VM Provisioning Trigger"
          echo "==============================================="
          
          attempts=0
          max_attempts=4
          wait_sec=5

          while [ $attempts -lt $max_attempts ]; do
            attempts=$((attempts+1))
            echo "  [*] Attempt $attempts/$max_attempts: Creating OCI Resource Manager Apply Job..."

            JOB_OUTPUT=$(/home/runner/bin/oci resource-manager job create-apply-job \
              --stack-id "$STACK_ID" \
              --execution-plan-strategy "AUTO_APPROVED" 2>&1 || true)

            if echo "$JOB_OUTPUT" | grep -q "TooManyRequests"; then
              echo "  [!] Rate limited by OCI API (HTTP 429). Retrying in ${wait_sec}s..."
              sleep $wait_sec
              wait_sec=$((wait_sec * 2))
              continue
            fi

            JOB_ID=$(echo "$JOB_OUTPUT" | jq -r '.data.id // empty' 2>/dev/null || echo "")

            if [ -n "$JOB_ID" ]; then
              echo ""
              echo "==============================================="
              echo "  [+] SUCCESS! OCI Job Created!"
              echo "  [+] Job ID: $JOB_ID"
              echo "  [i] Oracle Cloud Resource Manager is processing your VM creation asynchronously in the background."
              echo "==============================================="
              exit 0
            fi

            echo "  [!] OCI API returned an unexpected response:"
            echo "$JOB_OUTPUT"
            exit 1
          done

          echo "  [!] Exceeded max retries due to rate limiting (HTTP 429). Will try again next scheduled run."
          exit 0

